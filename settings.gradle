/*
 * GraphProbe
 * Settings configuration for the Gradle multi-project build
 */

// Plugin management - MUST be first
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'graphprobe'

include('framework', 'sample', 'codegen')
