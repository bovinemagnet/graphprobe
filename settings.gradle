/*
 * GraphProbe
 * Settings configuration for the Gradle multi-project build
 */

// Plugin management - MUST be first
pluginManagement {
    includeBuild('codegen') {
        name = 'graphprobe-codegen-plugin'
    }
    includeBuild('allure-gradle') {
        name = 'graphprobe-allure-plugin'
    }
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'graphprobe'

include('framework', 'sample', 'sample-codegen', 'codegen', 'allure-gradle')
